# Setup Wizard

The dashboard `Setup wizard` button shows active project root, active `.env`, writability, setup checks, doctor, and one paper cycle controls.

The wizard is intentionally paper-first. Live profile application still requires exact acknowledgement:

```text
I_UNDERSTAND_LIVE_TRADING_RISK
```
